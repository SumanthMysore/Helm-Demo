apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voteappchart.fullname" . }}-test-connection"
  labels:
    {{- include "voteappchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voteappchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
